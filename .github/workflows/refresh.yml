name: Refrescar Tablero Mercotruck
on:
  schedule:
    - cron: "*/20 * * * *"
  workflow_dispatch: {}
  push:
    branches: [ main ]
    paths: [ "build.py", "index_template.html", ".github/workflows/refresh.yml" ]
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: "pages"
  cancel-in-progress: false
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install requests cryptography
      - name: Generar datos + sitio
        env:
          AIRTABLE_PAT: ${{ secrets.AIRTABLE_PAT }}
          SITE_PASSWORD: ${{ secrets.SITE_PASSWORD }}
        run: python build.py
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: "public"
      - id: deploy
        uses: actions/deploy-pages@v4
